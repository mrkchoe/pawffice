-- Optional seed for a live Supabase project.
-- Demo mode ships its own in-browser seed (src/data/seed.ts) and does not need this.

-- Insert shelters/dogs after creating auth users + profiles.
-- Prefer the app's Demo Mode for the hackathon walkthrough.
